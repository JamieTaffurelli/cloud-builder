trigger:
  - master

stages:
  - stage: Build
    jobs:
      - template: build.yml
        parameters:
          poolName: "Default"

  - stage: Deploy
    jobs:
      - template: deploy.yml
        parameters:
          poolName: "Default"
    condition: and(succeeded(), ne(variables['Build.Reason'], 'PullRequest'))
