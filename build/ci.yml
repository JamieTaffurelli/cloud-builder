trigger:
  - master

stages:
  - stage: Build
    jobs:
      - template: build.yml
        parameters:
          poolName: "pool1"

  - stage: Deploy	
    jobs:	
      - template: deploy.yml	
        parameters:	
          poolName: "pool1"	
    condition: and(succeeded(), ne(variables['Build.Reason'], 'PullRequest'))